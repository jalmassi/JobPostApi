import { knexConfig } from "../config/dbConfig";
import { JobPost } from "../interfaces/JobPost";

    // id: number;
    // title: string;
    // description: string;
    // location: string;
    // hourlyPayRate: number;

    // createdAt: Date;
//     // updatedAt: Date;
// const jobposts: Array<JobPost> = [
//     { id: 1, title: 'software engineer', description: 'coding', location: 'Toronto', hourlyPayRate: 30, createdAt: (new Date(Date.now()), updatedAt: (new Date(Date.now())},
// ];